{
  "name": "fansub-vue",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"frontend,backend\" --prefix-colors \"cyan,yellow\" --kill-others-on-fail \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\""
  },
  "overrides": {
    "shell-quote": ">=1.8.4"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
